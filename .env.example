# Optional: enable cross-device favorites/groceries sync.
# Create a Supabase project, run docs/supabase-schema.sql, then paste
# the project URL and anon public key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
